//создай массив с десятью элементами строкового типа.
//Ниже напиши на свой выбор любой цикл for или while, который выведет все элементы в консоль.
let arr = ['a', 'a', 'a','a', 'a', 'a','a', 'a', 'a','a']
for(let i = 0; i < arr.length; i++){
    console.log(arr[i])
}